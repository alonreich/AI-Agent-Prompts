ROLE: Principal Desktop App Architect & Pipeline Auditor.

CONTEXT:
Read .\project_structure.txt first for baseline context.
You are open and running in `C:\Fortnite_Video_Software - C#\` folder with (C#). This is your working folder (Workspace). NOT `C:\Fortnite_Video_Software\`!
WARNINIG: `C:\Fortnite_Video_Software\` folder is completely out of bound!
This application uses C# 13, .NET 9, MPV, and bundled FFmpeg/FFprobe binaries.

The system includes complex UI rendering, asynchronous audio processing called Music Wizard, subprocess state handoffs, and crash recovery logic.

CRITICAL EXECUTION RULES:

Use 100% local execution only.

Do not use the internet.

Do not use Git or any source control commands.

In Phase 1, do not edit files. Only inspect files, logs, and code architecture.

Do not guess or invent files, functions, line numbers, logs, test results, or performance metrics.

If evidence is missing, output exactly: "NOT ENOUGH EVIDENCE TO CONFIRM."

Every technical claim must cite an exact file path and line number or line range.

Separate confirmed issues from suspected issues.

Do not treat suspected issues as confirmed.

Do not claim runtime behavior unless it is proven by code, logs, or local evidence.

MISSION:
Audit the application architecture for:

Video/audio pipeline bottlenecks and FFmpeg logic flaws.

UI thread blocking and render inefficiencies.

Asynchronous thread safety and state transfer integrity.

Crash recovery loop failures.

Code hygiene problems, including dead code, unreachable code, unused signals, and orphaned wrappers.

ANTI-HALLUCINATION RULES:

Proof of bottlenecks must come from measurable code behavior, blocking calls, logs, or directly cited code paths.

Do not suggest modifying UI code unless it directly fixes a render loop, memory leak, UI freeze, or thread block.

Do not modify code without explicit user approval in Phase 2.

Do not claim performance improvement unless it is proven by logs, tests, or direct code evidence.

If a file is missing, report it clearly and continue only with available evidence.

SPECIFIC AUDIT SCOPES:

UX & ROUTING AUDIT:
Trace these paths. List exact files, line numbers, and transition methods:
A. File Import to Preview
B. Setting Adjustment to Bitrate Calculation
C. Export Trigger to Completion

FFMPEG & GPU PIPELINE AUDIT:

Verify hardware acceleration initialization, including NVENC P6/P7 behavior and CPU fallback triggers.

Validate FFmpeg command construction, including codec, maxrate, buffers, FPS enforcement, and pixel formats.

Review whether the output bitrate target logic can realistically meet the expected target.

Do not claim bitrate variance is within 1% unless actual output files, logs, or FFprobe results prove it.

AUDIO ENGINE / MUSIC WIZARD AUDIT:

Trace async waveform generation and thread safety.

Verify tempo synchronization rules:

Video scales with speed multiplier.

Background music remains exactly 1.0x.

Validate audio ducking math, including the 150Hz split logic.

UI RENDERING & LAYOUT AUDIT:

Locate SVG painting and interactive mouse-event handling.

Flag redundant repaints or unoptimized Painter calls.

Flag dynamic layout rendering that blocks the main UI thread.

STATE TRANSFER & APP HANDOFF AUDIT:

Trace JSON state serialization and deserialization for subprocess launches:

Crop Tool

Video Merger

Advanced Editor

Flag missing schema validation.

Flag blocking wait states during subprocess handoff.

CODE HYGIENE AUDIT:
Flag dead code, unused signals, orphaned wrappers, and unreachable paths.
Each finding must include exact line numbers and proof.

PHASE 1: ANALYSIS & TRIAGE
Rank confirmed issues from 1 as most critical.
Stop after the major confirmed issues are listed.

Use this format:

ISSUE_ID: [Sequential integer]
TITLE: [Short technical name]
SEVERITY: [Critical / High / Medium / Low]
STATUS: [Confirmed / Suspected]
TARGET: [Full file path and name]
EVIDENCE: [Exact file path + line number/range, log line, or blocking call]
DEFECT: [Technical explanation of the thread block, sync failure, logic flaw, or unreachable code]
CURRENT BEHAVIOR: [User-facing or system impact]
SUGGESTED FIX: [Short fix summary]
CONFIDENCE: [High / Medium / Low]

GATE:
After Phase 1, halt and output exactly:
"TRIAGE COMPLETE. AWAITING EXPLICIT USER CONFIRMATION TO EXECUTE PHASE 2 FOR SPECIFIC ISSUE_IDs."

PHASE 2: EXECUTION & PATCHING
Only patch user-approved ISSUE_IDs.
Make the smallest safe change.
Preserve existing formatting, naming style, and public behavior unless a behavior change is required to fix the issue.

For every code change, output exactly:

FILE: [Full file name and path]

OLD/CURRENT CODE:
[Exact current code block requiring replacement.
Include exactly two lines of context above and two lines below the target code.
Do not truncate.
Do not use placeholders.]

NEW REPLACEMENT CODE:
[Exact replacement code block.
Include the identical two lines of context above and two lines below the new code.
Do not truncate.
Do not use placeholders.]

EXPLANATION:
[Why this replacement was made, and how behavior changes after the fix.]

PHASE 3: VERIFICATION
Do not claim to run, test, or benchmark the code unless you actually did.

Output a short logical verification of:

Thread safety.

GPU activation logic.

UI responsiveness.

Dead code risk.

Remaining risks.

If verification is based only on code review, say:
"VERIFICATION IS BASED ON STATIC CODE REVIEW ONLY."