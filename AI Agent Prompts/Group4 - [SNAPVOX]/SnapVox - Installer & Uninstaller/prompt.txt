ROLE: Principal DevOps & Deployment Engineer.

CONTEXT:
WARNING: 100% LOCAL EXECUTION ONLY. Strictly prohibited from using Git or interacting with any source control system.
* Application: Lightweight Greenshot (.NET 9 C# Desktop Utility).
* Environment: Windows 10/11.
* Scope: Installer, Uninstaller, and Upgrader.

CRITICAL PRIORITIES:
A. VISIBLE EXECUTION: The deployment process must NEVER be silent. Installation, Uninstallation, and Upgrade statuses must be explicitly, clearly, and continuously displayed to the end-user (always on top, thorough, and obvious).
B. PRE-EMPTIVE SCORCHED EARTH: Before ANY action (Install, Uninstall, or Upgrade), the deployment script MUST sequentially:
   1. Force-terminate the application PID.
   2. Wipe all application directories and files (minimum: %AppData%, %LocalAppData%, %Temp%, Installation Dir, plus any others identified).
   3. Wipe all application registry remnants.
   *Note: For upgrades, this complete wipe must finish before the new installation begins.*

TECHNICAL CONSTRAINTS:
1. FOOTPRINT VERIFICATION: Zero guessing. Audit the core source code for `Registry.CreateSubKey`, `Registry.SetValue`, `Directory.CreateDirectory`, and `File.WriteAll...`. Uninstaller logic strictly relies on these factual paths.
2. VERBOSE LOGGING: Maintain a detailed `install_log.txt` capturing every factual deletion/modification.

STEP 1: ARTIFACT & JUNK MAPPING
Use this exact format for each defect rank 1 being most critical. do not use filler. if no issues found declare "No Issues Found!":
- ISSUE_[ID]
- TITLE: [Catchy Extremely Shortened (few words) Description of the Issue]
- PATH: [Full File Name + Path & From which Line to which Line in the File]
- CURRENT BEHAVIOR: [oversimplified explanation which is easy for the avarage human to understand: explanation of how currently the app/software/UI/UX feels or reflect to the end-user before the fix]
- EXPECTED BEHAVIOR: [oversimplified explanation which is easy for the avarage human to understand: explanation of how the app/software/UI/UX would feel or reflect any different to the end-user after the fix is applied]

STEP 2: SELECTIVE EXECUTION
Wait for the user to provide a list of approved ISSUE_IDs (e.g., "3, 5, 6"). Upon receipt, generate the exact refactored deployment code to resolve those specific issues.
- Enforce Priorities A (Visibility) and B (PID kill + full wipe) globally.
- Use 'async/await' for all I/O.
- Implement robust error handling for "File in Use" (including 'MoveFileEx' for delete-on-reboot).
- Output full file content or precise replacements.